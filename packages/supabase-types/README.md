# @fynestudy/supabase-types

Generated TypeScript types for the Supabase database schema.

## Regenerate

```bash
pnpm dlx supabase gen types typescript --project-id <project-ref> > packages/supabase-types/index.ts
```

This package is intentionally empty in Phase 1 — no business tables yet. It will be populated starting Phase 2.
