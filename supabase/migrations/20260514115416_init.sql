-- Phase 1 placeholder.
-- Ensures the migrations table exists on the linked dev project and that
-- `supabase db push --linked` succeeds against an otherwise empty schema.
-- Real schema work starts in Phase 2 (auth) and Phase 3 (courses).
select 1;
