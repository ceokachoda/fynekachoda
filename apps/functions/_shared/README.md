# _shared

Modules imported by edge functions. Never deployed as standalone functions.

Phase 1 has no shared modules yet — they land starting Phase 2 (`supabase.ts`, `auth.ts`, `audit.ts`, `ratelimit.ts`, etc. per `docs/file-structure.md`).
